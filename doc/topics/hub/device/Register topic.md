# Register topic

Register topic have next format: `{direction}{hub}/{device}/{reg}`

Where:

| Part | Description | 
| --- | --- |
| direction| Message direction, `>` - from network to device, `<` - from device to network | 
| hub | Hub token (MAC address usually) |
| device | Device serial |
| reg | Register number | 

