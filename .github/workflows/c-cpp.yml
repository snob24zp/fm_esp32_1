name: Release CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  CppCheck:
    name: 'Статическое тестирование кода'
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: recursive
    - run: ./ci/00-code-check.sh
    - uses: actions/upload-artifact@v2
      with:
        name: 'lintcheck'
        path: out/code-check.log
  Build:
    name: 'Сборка прошивки обновления'
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: recursive
    - run: ./ci/00-build.sh
    - uses: actions/upload-artifact@v2
      with:
        name: 'fw.fs'
        path: out/fw.fs
    - uses: actions/upload-artifact@v2
      with:
        name: 'AR.FW.latest.uebf'
        path: out/AR.FW.latest.uebf
  BuildBinaries:
    name: 'Сборка ESP32 прошивки'
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: recursive
    - run: ./ci/01-build-esp32-binary.sh
    - uses: actions/upload-artifact@v2
      with:
        name: 'AR.BINS.latest.zip'
        path: out/bins/AR.BINS.latest.zip
  Test:
    name: 'Тестирование'
    runs-on: self-hosted
    needs: Build
    steps:
    - uses: actions/download-artifact@v2
      with:
        name: AR.FW.latest.uebf
        path: out
    - run: ./ci/01-tests-simu.sh
    - uses: actions/upload-artifact@v2
      with:
        name: 'tests-summary'
        path: out/tests/
  GenDoc:
    name: 'Генерация документации'
    runs-on: self-hosted
    needs: [Build, BuildBinaries]
    steps:
    - run: ./ci/00-gendoc.sh
    - uses: actions/upload-artifact@v2
      with:
        name: 'docs-summary'
        path: out/docs/
  Release:
    name: 'Подготовка релиза'
    runs-on: self-hosted
    needs: [Test, GenDoc, CppCheck, BuildBinaries]
    # if: github.event_name == 'push' && (contains(github.ref, '/heads/master') || contains(github.ref, '/tags/v')))
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 5
          submodules: recursive
      - uses: actions/download-artifact@v2
        with:
          path: artifacts
      - run: ./ci/02-release-prepare.sh
